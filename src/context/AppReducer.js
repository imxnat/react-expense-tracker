/* A reducer is basically how we specify the apps state changings in response to
certain actions 
*/ 

export default (state, action) => {
    switch(action.type) {
        default:
         return state;
    }
}
